// Sector CANOPY rasters — generated by work/bake_sector.py.
// Area-averaged out of the finest ArcGIS World Imagery frame covering each cell
// (kt22/aerial-close 0.293 m/px, red-dog/aerial 0.488, kt22/aerial-exact 0.488,
// red-dog/aerial-2 1.5625) at 2.0 m/px, registered 1:1 with the DEM by bbox
// arithmetic alone. Value = canopy fraction 0..255.
// Rect grids: row 0 = SOUTH edge, +x east, origin at (x0, y0) cell CORNER.//
// STUBBED by poi-lab tools/export-red-dog (specs/0001 D13).
// D13.1 — STUB, never delete: canopy.mjs:8 imports the named symbol. Every sector raster starts at x0 <= -800; no sector raster touches CORE.
// The original module is 3,866,077 bytes of base64 raster.

export const SECTOR_CANOPIES = [];
