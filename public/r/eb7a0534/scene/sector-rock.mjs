// Sector BARE-ROCK rasters — generated by work/bake_sector.py.
// Red Dog is a tree mountain; KT-22 is a rock mountain. The summer aerials show
// exactly which ground is bare volcanic rock: not canopy, not snow-white,
// warm-neutral, mid luminance. Same frames, same 2.0 m/px grid and the same
// bbox-only registration as the canopy rasters. Value = rock fraction 0..255.//
// STUBBED by poi-lab tools/export-red-dog (specs/0001 D13).
// D13.2 — STUB, never delete: rock.mjs:19 imports the named symbol.
// The original module is 3,866,037 bytes of base64 raster.

export const SECTOR_ROCKS = [];
